[uwsgi]
project = application
base = /v-templates

chdir = %(base)/%(project)
home = %(base)/%(project)
module = %(project).config.wsgi:application

master = true
processes = 2

socket = %(base)/%(project)/%(project).sock
chmod-socket = 664
vacuum = true